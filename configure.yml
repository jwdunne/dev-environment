---
- name: Configure my Debian system
  hosts: 127.0.0.1
  connection: local
  become: yes

  roles:
    - shell
    - python
    - editor
    # Ansible
    # Node
    # Molecule
    # Markdown
    # ZSH
    # Rust
