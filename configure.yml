---
- name: Configure my Debian system
  hosts: 127.0.0.1
  connection: local
  become: yes

  vars_files:
    - config/editor.yml
    - config/python.yml

  roles:
    - editor
    - python
    # Node
    # Molecule
    # Markdown
    # ZSH
    # Rust
